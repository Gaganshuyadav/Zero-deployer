import React from 'react';
import DashboardLayout from "@/components/layout/DashboardLayout";

const Settings = () => {
  return (
    <div>Settings</div>
  )
}

export default DashboardLayout()(Settings);
